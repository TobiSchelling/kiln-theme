-- {{ display_name }} — lualine theme
-- Generated by kiln-theme v{{ version }}
--
-- Installation: copy to ~/.config/nvim/lua/lualine/themes/kiln-{{ variant }}.lua
-- Auto-discovered when colorscheme is kiln-{{ variant }}

local colors = {
  crust    = "{{ colors.crust.hex }}",
  mantle   = "{{ colors.mantle.hex }}",
  surface0 = "{{ colors.surface0.hex }}",
  surface1 = "{{ colors.surface1.hex }}",
  text     = "{{ colors.text.hex }}",
  subtext1 = "{{ colors.subtext1.hex }}",
  comment  = "{{ colors.comment.hex }}",
  blue     = "{{ colors.blue.hex }}",
  peach    = "{{ colors.peach.hex }}",
  mauve    = "{{ colors.mauve.hex }}",
  yellow   = "{{ colors.yellow.hex }}",
  red      = "{{ colors.red.hex }}",
  green    = "{{ colors.green.hex }}",
}

return {
  normal = {
    a = { bg = colors.blue, fg = colors.crust, gui = "bold" },
    b = { bg = colors.surface1, fg = colors.text },
    c = { bg = colors.mantle, fg = colors.subtext1 },
  },
  insert = {
    a = { bg = colors.peach, fg = colors.crust, gui = "bold" },
    b = { bg = colors.surface1, fg = colors.text },
    c = { bg = colors.mantle, fg = colors.subtext1 },
  },
  visual = {
    a = { bg = colors.mauve, fg = colors.crust, gui = "bold" },
    b = { bg = colors.surface1, fg = colors.text },
    c = { bg = colors.mantle, fg = colors.subtext1 },
  },
  command = {
    a = { bg = colors.yellow, fg = colors.crust, gui = "bold" },
    b = { bg = colors.surface1, fg = colors.text },
    c = { bg = colors.mantle, fg = colors.subtext1 },
  },
  replace = {
    a = { bg = colors.red, fg = colors.crust, gui = "bold" },
    b = { bg = colors.surface1, fg = colors.text },
    c = { bg = colors.mantle, fg = colors.subtext1 },
  },
  inactive = {
    a = { bg = colors.mantle, fg = colors.comment, gui = "bold" },
    b = { bg = colors.mantle, fg = colors.comment },
    c = { bg = colors.crust, fg = colors.comment },
  },
}
