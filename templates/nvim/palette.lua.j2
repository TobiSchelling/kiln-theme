-- {{ display_name }} — Neovim palette
-- Generated by kiln-theme v{{ version }}

local M = {}

M.variant = "{{ variant }}"

M.colors = {
  {% for name, c in colors.items() %}
  {{ name }} = "{{ c.hex }}",
  {% endfor %}
}

return M
