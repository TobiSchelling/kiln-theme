# {{ display_name }} — Tide prompt theme
# Generated by kiln-theme v{{ version }}
#
# Source this file or copy into your fish config:
#   source ~/.config/fish/themes/kiln-{{ variant }}-tide.fish

# --- Prompt chrome ---
set -U tide_prompt_color_frame_and_connection {{ colors.surface2.hex_stripped }}
set -U tide_prompt_color_separator_same_color {{ colors.surface2.hex_stripped }}

# --- Character (prompt symbol) ---
set -U tide_character_color {{ colors.peach.hex_stripped }}
set -U tide_character_color_failure {{ colors.red.hex_stripped }}

# --- PWD ---
set -U tide_pwd_bg_color {{ colors.surface0.hex_stripped }}
set -U tide_pwd_color_anchors {{ colors.peach.hex_stripped }}
set -U tide_pwd_color_dirs {{ colors.subtext1.hex_stripped }}
set -U tide_pwd_color_truncated_dirs {{ colors.comment.hex_stripped }}

# --- Git ---
set -U tide_git_bg_color {{ colors.surface0.hex_stripped }}
set -U tide_git_bg_color_unstable {{ colors.surface1.hex_stripped }}
set -U tide_git_bg_color_urgent {{ colors.red.hex_stripped }}
set -U tide_git_color_branch {{ colors.sapphire.hex_stripped }}
set -U tide_git_color_conflicted {{ colors.red.hex_stripped }}
set -U tide_git_color_dirty {{ colors.yellow.hex_stripped }}
set -U tide_git_color_operation {{ colors.rose.hex_stripped }}
set -U tide_git_color_staged {{ colors.green.hex_stripped }}
set -U tide_git_color_stash {{ colors.mauve.hex_stripped }}
set -U tide_git_color_untracked {{ colors.teal.hex_stripped }}
set -U tide_git_color_upstream {{ colors.sky.hex_stripped }}

# --- Status ---
set -U tide_status_bg_color {{ colors.surface0.hex_stripped }}
set -U tide_status_bg_color_failure {{ colors.red.hex_stripped }}
set -U tide_status_color {{ colors.green.hex_stripped }}
set -U tide_status_color_failure {{ colors.crust.hex_stripped }}

# --- Command duration ---
set -U tide_cmd_duration_bg_color {{ colors.surface0.hex_stripped }}
set -U tide_cmd_duration_color {{ colors.yellow.hex_stripped }}

# --- Context (user@host) ---
set -U tide_context_bg_color {{ colors.surface0.hex_stripped }}
set -U tide_context_color_default {{ colors.subtext1.hex_stripped }}
set -U tide_context_color_root {{ colors.red.hex_stripped }}
set -U tide_context_color_ssh {{ colors.sky.hex_stripped }}

# --- Jobs ---
set -U tide_jobs_bg_color {{ colors.surface0.hex_stripped }}
set -U tide_jobs_color {{ colors.lavender.hex_stripped }}

# --- Time ---
set -U tide_time_bg_color {{ colors.surface0.hex_stripped }}
set -U tide_time_color {{ colors.comment.hex_stripped }}

# --- OS ---
set -U tide_os_bg_color {{ colors.surface0.hex_stripped }}
set -U tide_os_color {{ colors.text.hex_stripped }}

# --- Vi mode ---
set -U tide_vi_mode_bg_color_default {{ colors.blue.hex_stripped }}
set -U tide_vi_mode_color_default {{ colors.crust.hex_stripped }}
set -U tide_vi_mode_bg_color_insert {{ colors.green.hex_stripped }}
set -U tide_vi_mode_color_insert {{ colors.crust.hex_stripped }}
set -U tide_vi_mode_bg_color_replace {{ colors.red.hex_stripped }}
set -U tide_vi_mode_color_replace {{ colors.crust.hex_stripped }}
set -U tide_vi_mode_bg_color_visual {{ colors.mauve.hex_stripped }}
set -U tide_vi_mode_color_visual {{ colors.crust.hex_stripped }}

# --- Private mode ---
set -U tide_private_mode_bg_color {{ colors.overlay0.hex_stripped }}
set -U tide_private_mode_color {{ colors.text.hex_stripped }}

# --- Language versions ---
set -U tide_node_bg_color {{ colors.surface0.hex_stripped }}
set -U tide_node_color {{ colors.green.hex_stripped }}
set -U tide_python_bg_color {{ colors.surface0.hex_stripped }}
set -U tide_python_color {{ colors.yellow.hex_stripped }}
set -U tide_rustc_bg_color {{ colors.surface0.hex_stripped }}
set -U tide_rustc_color {{ colors.maroon.hex_stripped }}
set -U tide_go_bg_color {{ colors.surface0.hex_stripped }}
set -U tide_go_color {{ colors.sky.hex_stripped }}
set -U tide_ruby_bg_color {{ colors.surface0.hex_stripped }}
set -U tide_ruby_color {{ colors.red.hex_stripped }}
set -U tide_java_bg_color {{ colors.surface0.hex_stripped }}
set -U tide_java_color {{ colors.peach.hex_stripped }}
set -U tide_php_bg_color {{ colors.surface0.hex_stripped }}
set -U tide_php_color {{ colors.lavender.hex_stripped }}

# --- Tools ---
set -U tide_docker_bg_color {{ colors.surface0.hex_stripped }}
set -U tide_docker_color {{ colors.sapphire.hex_stripped }}
set -U tide_kubectl_bg_color {{ colors.surface0.hex_stripped }}
set -U tide_kubectl_color {{ colors.teal.hex_stripped }}
set -U tide_aws_bg_color {{ colors.surface0.hex_stripped }}
set -U tide_aws_color {{ colors.gold.hex_stripped }}
set -U tide_gcloud_bg_color {{ colors.surface0.hex_stripped }}
set -U tide_gcloud_color {{ colors.sapphire.hex_stripped }}
set -U tide_terraform_bg_color {{ colors.surface0.hex_stripped }}
set -U tide_terraform_color {{ colors.mauve.hex_stripped }}
set -U tide_nix_shell_bg_color {{ colors.surface0.hex_stripped }}
set -U tide_nix_shell_color {{ colors.sky.hex_stripped }}
